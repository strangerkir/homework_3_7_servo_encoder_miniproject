 # Servo Encoder Miniproject

 A small project for controlling a servo motor and reading an encoder.

# Features
- Reading encoder input and changing servo angle accordingly
- Slow mode on/off on encoder's button - when enabled, servo moves 2 times slower
- Buzzer sound at ranges - when servo's min/max ranges are reached, buzzer sound is played